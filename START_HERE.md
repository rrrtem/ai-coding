# START HERE — Подготовка к воркшопу

> Время: \~30 минут. Сделайте за 2-3 дня до воркшопа. На самом воркшопе мы не будем ждать установку.

---

## Шаг 0. Оформите подписку на AI-агент

Нужна **одна** из двух:

- **Claude Pro или Max** ([claude.ai](https://claude.ai)) — Pro хватает для воркшопа. Max — больше лимитов, нужен если планируете использовать Claude Code активно каждый день.
- **ChatGPT** — если хотите использовать [Codex ](https://github.com/openai/codex)вместо Claude Code.

Разница религиозная, выбирайте что хотите.\
Берите подписау за 20 пока, потом сделаете апдейте когда упретесь в лимиты.\
На воркшопе мы работаем с Claude Code, но Codex — рабочая альтернатива.

---

## Шаг 1. Создайте аккаунты (5 мин)

| \# | Сервис | Зачем | Ссылка |
| --- | --- | --- | --- |
| 1 | **GitHub** | Хранение кода | [github.com/signup](https://github.com/signup) |
| 2 | **Vercel** | Хостинг сайта (бесплатно) | [vercel.com](https://vercel.com) — войти через GitHub |
| 3 | **Supabase** | База данных (бесплатно) | [supabase.com](https://supabase.com) — войти через GitHub |

> Vercel и Supabase входите **через GitHub** — так проще и не нужно запоминать отдельный пароль.

---

## Шаг 2. Откройте терминал

- **macOS:** нажмите `Cmd + Пробел`, наберите "Terminal", нажмите Enter
- **Windows:** установите WSL2 (`wsl --install` в PowerShell от администратора, потребуется перезагрузка), затем откройте Ubuntu из меню Пуск

---

## Шаг 3. Установите Claude Code и запустите настройку (15-20 мин)

Скопируйте и вставьте эту команду в терминал:

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

После установки выполните эту команду, чтобы `claude` стал доступен в текущем окне терминала:

```bash
source ~/.zshrc   # macOS
source ~/.bashrc  # Windows (WSL)
```

Проверьте что установилось:

```bash
claude --version
```

Теперь запустите Claude Code:

```bash
  claude --dangerously-skip-permissions 
```

При первом запуске он попросит войти в аккаунт Claude — следуйте инструкциям на экране. Когда откроется чат, вставьте этот текст:

```
Склонируй репозиторий https://github.com/rrrtem/ai-coding и выполни все шаги из CLAUDE.md по порядку.
```

**Что произойдёт** (15-25 минут, в зависимости от скорости интернета):

- Claude скачает репозиторий с инструкциями
- Начнёт устанавливать базовые инструменты (Node.js, Git, GitHub CLI, Vercel, Supabase и др.) — это \~10 шагов
- Будет объяснять каждый шаг: что ставится, зачем, как связано с остальным
- Иногда попросит вас что-то подтвердить (например, авторизовать GitHub в браузере) — просто следуйте его указаниям
- Спросит, какие проекты вам интересны (сайты, боты, мобильные приложения) — и доустановит нужные инструменты
- По завершении создаст файл `setup-report.md` с отчётом

---

## Шаг 4. Отправьте отчёт

Когда Claude закончит, в корне проекта появится файл `setup-report.md`.

Отправьте его ведущему воркшопа — так мы убедимся что у вас всё готово.

---

## Шаг 5. Установите Supersets (GUI для Claude Code)

Supersets — графический интерфейс для Claude Code. На воркшопе мы будем работать в нём.

Скачайте с [supersets.sh, о](https://supersets.sh)ткройте и войдите через GitHub

---

## Что-то пошло не так?

- **Claude завис или тупит?** Закройте его (`Ctrl + C`), откройте заново (`claude`) и скажите: "Продолжи настройку с шага \[номер\]". Он проверит что уже стоит и продолжит с нужного места.
- **Хотите пропустить шаг?** Скажите Claude "пропусти" — он перейдёт к следующему. Всё пропущенное можно доделать позже.
- **Ничего не помогает?** Откройте `prework/falllback-manual-checklist.md` — там пошаговая инструкция для ручной установки.

После воркшопа этот проект останется как шпаргалка. В /docs лежат всякие подсказки.\
\
На воркшопе мы создадим новый проект для вашего продукта.